# OpenAI Configuration (Server-side only - DO NOT use NEXT_PUBLIC prefix)
OPENAI_API_KEY=your_openai_api_key_here

# Application Configuration
NODE_ENV=development

# API Backend URL (if different from default)
# NEXT_PUBLIC_API_URL=https://sweekarme.in/oecweb/api

# Optional: Database Configuration (if implementing)
# DATABASE_URL=your_database_url_here

# Optional: Email Service (if implementing)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your_email@example.com
# SMTP_PASS=your_email_password
